type Query {
  plants: [Plant!]!
  plant(id: ID!): Plant
  schedule: Schedule
}

type Plant {
  id: ID!
  name: String
  species: String
  schedule: Schedule
}

type Schedule {
  id: ID!
  date: String!
  plants: [Plant]
}

schema {
  query: Query
}
